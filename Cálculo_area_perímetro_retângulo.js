function area_perimetro_rentangulo(base, altura) {
    let area = base * altura
    let perimetro = 2 * (base + altura)

    return console.log(`A area do retângulo é ${area} e seu perímetro é ${perimetro}`)
}

area_perimetro_rentangulo(11, 7)